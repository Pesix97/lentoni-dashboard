# I file scaricati ad ogni esecuzione sono input temporanei, non stato del progetto.
# EA ci mette dentro campi volatili (teamId, kitId) che cambiano ad ogni chiamata:
# tenerli sotto controllo di versione significherebbe un commit ogni due ore a vuoto.
raw/
